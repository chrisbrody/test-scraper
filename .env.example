# ==============================================================================
# Multi-Vendor Product Scraper - Environment Configuration
# ==============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control - it contains sensitive credentials

# ==============================================================================
# SUPABASE DATABASE CONFIGURATION (Required)
# ==============================================================================
# Your Supabase project URL
# Format: https://your-project-id.supabase.co
SUPABASE_URL=your_supabase_url_here

# Your Supabase service role key (NOT the anon key)
# This key has full access to your database - keep it secure!
# Find it in: Supabase Dashboard > Project Settings > API > service_role key
SUPABASE_KEY=your_supabase_service_role_key_here

# ==============================================================================
# PROXY CONFIGURATION (Optional but Highly Recommended for Production)
# ==============================================================================

# Enable or disable proxy rotation
# Set to 'true' to enable, 'false' to disable
# Default: false
PROXY_ENABLED=false

# Type of proxy (for documentation purposes)
# Options: residential, datacenter
# Residential proxies are recommended for JavaScript-heavy sites
# Datacenter proxies are cheaper and work for simpler sites
PROXY_TYPE=residential

# Comma-separated list of proxy servers
# Formats supported:
#   - Simple: ip:port (e.g., 192.168.1.1:8080)
#   - With protocol: http://ip:port or https://ip:port
#   - With auth embedded: http://user:pass@ip:port
# Example: proxy1.example.com:8080,proxy2.example.com:8080,proxy3.example.com:8080
PROXY_LIST=

# Proxy authentication (optional)
# If your proxies require username/password, set them here
# These credentials will be applied to all proxies in PROXY_LIST
# Leave empty if authentication is embedded in PROXY_LIST or not required
PROXY_USERNAME=
PROXY_PASSWORD=

# Delay between requests (in seconds)
# Adds random delay to mimic human browsing behavior
# Helps avoid detection and rate limiting
# Recommended: 1.0 - 3.0 seconds
# Default: 0.5
PROXY_ROTATION_DELAY=1.5

# ==============================================================================
# PROXY SERVICE PROVIDER EXAMPLES
# ==============================================================================
# Uncomment and modify the section for your proxy provider

# --- Bright Data (Premium) ---
# PROXY_ENABLED=true
# PROXY_TYPE=residential
# PROXY_LIST=brd.superproxy.io:22225
# PROXY_USERNAME=brd-customer-your_customer_id-zone-your_zone
# PROXY_PASSWORD=your_password

# --- Smartproxy ---
# PROXY_ENABLED=true
# PROXY_TYPE=residential
# PROXY_LIST=gate.smartproxy.com:7000
# PROXY_USERNAME=your_username
# PROXY_PASSWORD=your_password

# --- ScraperAPI (Managed Service) ---
# PROXY_ENABLED=true
# PROXY_LIST=proxy-server.scraperapi.com:8001
# PROXY_USERNAME=scraperapi
# PROXY_PASSWORD=your_api_key

# --- Oxylabs ---
# PROXY_ENABLED=true
# PROXY_TYPE=residential
# PROXY_LIST=pr.oxylabs.io:7777
# PROXY_USERNAME=customer-your_customer_id
# PROXY_PASSWORD=your_password

# ==============================================================================
# NOTES
# ==============================================================================
# 1. Always use proxies for production scraping to avoid IP bans
# 2. Residential proxies are best for sites with bot detection (Bernhardt, etc.)
# 3. Monitor proxy health and replace failing proxies
# 4. Combine proxies with PROXY_ROTATION_DELAY for best results
# 5. Test your proxy configuration before running production scrapes
